import styled from 'styled-components';

const Avatar = styled.div`
  align-items: center;
  display: flex;

  .avatar__image {
    border-radius: 48px;
    height: 48px;
    object-fit: cover;
    width: 48px;
  }
`;

export default Avatar;
